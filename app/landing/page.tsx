import LandingScreen from '@/components/LandingScreen';

/**
 * /landing — explicit landing route (same as root).
 * Useful when QR codes point here directly.
 */
export default function LandingPage() {
  return <LandingScreen />;
}
